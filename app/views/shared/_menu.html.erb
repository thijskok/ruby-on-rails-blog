<nav class="navbar is-light">
  <div class="container">
      <div class="navbar-brand">
          <a class="navbar-item" href="../">
              <img src="https://img.logoipsum.com/332.svg" width="57" alt="Logo">
          </a>
          <span class="navbar-burger burger" data-target="navbarMenu">
            <span></span>
            <span></span>
            <span></span>
          </span>
      </div>
      <div id="navbarMenu" class="navbar-menu">
          <div class="navbar-start">
            <%= link_to 'Home', root_path, class: "navbar-item #{'is-active' if current_page?(root_path)}" %>
            <%= link_to 'Articles', articles_path, class: "navbar-item #{'is-active' if request.path == '/articles' || request.path.include?('/articles')}" %>
          </div>
          <div class="navbar-end">
              <% if user_signed_in? %>
                <%= link_to 'Account', edit_user_registration_path, class: "navbar-item #{'is-active' if current_page?(edit_user_registration_path)}" %>
                <%= link_to 'Logout', destroy_user_session_path, data: {turbo_method: :delete}, class: "navbar-item" %>
              <% else %>
                <%= link_to 'Sign up', new_user_registration_path, class: "navbar-item #{'is-active' if current_page?(new_user_registration_path)}" %>
                <%= link_to 'Login', new_user_session_path, class: "navbar-item #{'is-active' if current_page?(new_user_session_path)}" %>
              <% end %>
          </div>
      </div>
  </div>
</nav>
